<% if record.errors.any? %>
  <div id="error_explanation">
    <h2>
<!--      <%#= pluralize(record.errors.count, "error") %> prohibited this <%#= record.class.to_s.downcase %> from being saved:-->

      <%= t('.header', count: record.errors.count) %>
    </h2>

    <ul>
      <% record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>