<h1><%= t('.header') %></h1>

<table class="table table-striped table-bordered table-hover table-sm">
  <thead class="thead-dark">
    <tr>
      <% humanize_attribute_names(%i[title level category author]).each do |field_name| %>
        <th scope="col"><%= field_name %></th>
      <% end %>
      <th><%= t('.questions') %></th>
      <th><%= t('.actions') %></th>
    </tr>
  </thead>

  <tbody>
    <%= render @tests %>
  </tbody>
</table>
<br>
